import { createStore } from 'vuex';
import loginStore from './modules/loginStore';
export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    loginStore
  }
});
